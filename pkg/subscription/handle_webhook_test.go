package subscription_test

import (
	"io/ioutil"

	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
	"github.com/sirupsen/logrus"
)

var _ = Describe("", func() {

	logrus.SetOutput(ioutil.Discard)

	// TODO Vars
	//	var (
	//	)

	// TODO Before each
	//	BeforeEach(func() {
	//	})

	// TODO Mocks
	//	mockRun := func(err error, arguments ...interface{}) {
	//		gqlMock.On("FuncName", arguments...).Return(err)
	//	}
	Context("pau", func() {
		It("", func() {
			// Given

			// When

			// Then

			Expect(nil).Should(BeNil())
		})
	})
})
